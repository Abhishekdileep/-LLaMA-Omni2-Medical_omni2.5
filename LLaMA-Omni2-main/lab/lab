

import numpy as np
import pandas as pd

print("NumPy version:", np.__version__)
print("Pandas version:", pd.__version__)

# Check CSV files in the current working directory
import os

csv_files = [f for f in os.listdir() if f.lower().endswith(".csv")]

print("\nCSV files in working directory:")
for file in csv_files:
    print("-", file)

if csv_files:
    print("\nDataset CSV is present.")
else:
    print("\nNo CSV dataset found in the working directory.")